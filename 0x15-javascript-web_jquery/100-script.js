// Function to update the text color of the <header> element to red (#FF0000)
function updateHeaderTextColor() {
  // Select the <header> element using document.querySelector
  const headerElement = document.querySelector('header');

  // Update the text color to red
  headerElement.style.color = '#FF0000';
}

// Call the function to update the header text color
updateHeaderTextColor();
